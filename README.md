# Python Tasks Telegram Bot

---

To install requirements run: 


`pip install -r requirments.txt`

---

Copy file `example.env` into `.env` and replace `TOKEN` with your token from the telegram

---

To run:

`python main.py`